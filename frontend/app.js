const React = require('react');
const ReactDOM = require('react-dom');
const FlowSessionFrame = require('./react/SessionFrame');

ReactDOM.render(
    <FlowSessionFrame/>, document.getElementById("root")
);